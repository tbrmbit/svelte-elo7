import SectionJobs from './SectionJobs.svelte';
import {fireEvent, render} from '@testing-library/svelte';


describle('SectionJobs', () => {
  it('fetches data from server when server returns a successful response', async () => {
    return true;
  })
});